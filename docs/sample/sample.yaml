paths:
  api/v1/test:
    post:
      description: Sample documentation

      produces:
        - application/json

      parameters:
        - name: test
          description: Test object
          in: body
          required: true
          type: string
          schema:
            $ref: '#/definitions/Sample'

      responses:
        200:
          description: test
